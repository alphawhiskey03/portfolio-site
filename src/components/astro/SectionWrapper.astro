---
import { cn } from "@/lib/utils";
const { className, id } = Astro.props;
---

<section
  class={cn(
    `flex flex-col lg:flex-row lg:justify-between lg:items-center my-8`,
    className,
  )}
  id={id}
>
  <slot />
</section>
